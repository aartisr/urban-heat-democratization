
# Lean runtime dependencies for the Vercel FastAPI function.
# The full local research, raster-export, and test toolchain is in
# requirements-research.txt. Keeping those optional tools out of this file is
# necessary to keep the serverless function below Vercel's 500 MB limit.
setuptools<81
fastapi==0.110.0
uvicorn[standard]==0.27.1
numpy==1.26.4
scipy==1.11.4
networkx==3.2.1
# 2.6.1 pins pydantic-core 2.16.2, which has no compatible wheel for current
# Vercel Python builders and falls back to a failing Rust source build.
pydantic==2.13.4
