import { copyFile, mkdir, readFile, writeFile } from "node:fs/promises";
import { resolve } from "node:path";

const root = resolve(import.meta.dirname, "..");
const config = JSON.parse(await readFile(resolve(root, "seo/site.config.json"), "utf8"));
const out = resolve(root, "site");
const esc = (value) => String(value).replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;");
const main = config.mainSiteUrl.replace(/\/$/, "") + "/";
const pages = config.githubPagesUrl.replace(/\/$/, "") + "/";
const repoDocs = `${config.repositoryUrl}/tree/main/docs/wiki`;
const wikiHome = `${pages}wiki/`;

const guides = [
  {
    slug: "",
    eyebrow: "Public-interest urban heat research",
    title: "Heat evidence belongs in public.",
    deck: "A field guide for people who need to see local heat patterns, understand the evidence, and help shape thoughtful action on cooling, shade, and public investment.",
    image: "screenshots/home.png",
    imageAlt: "Urban Heat Democratization home experience",
    primary: ["Explore the live platform", main],
    secondary: ["Begin with Boston", `${main}cities/boston`],
    sections: [
      ["Begin with a question, not a conclusion", "A heat map should start a conversation—not end one. Learn where a pattern comes from, what it leaves out, and what local knowledge is needed next."],
      ["Follow the evidence", "Move from city layers to sources, methods, caveats, and a documented record. The work distinguishes observations, proxies, and exploratory scenarios."],
      ["Shape action openly", "Use transparent scenarios to compare questions, constraints, and tradeoffs before public resources are committed."],
    ],
    links: [
      ["Live city atlas", `${main}cities/boston`, "Inspect the bundled Boston study and its caveats."],
      ["Scenario workspace", `${main}scenarios`, "Compare transparent what-if options."],
      ["Research wiki", wikiHome, "Read the full methods, equations, evidence, and governance field guide."],
      ["Boston study guide", `${config.repositoryUrl}/blob/main/docs/BOSTON_STUDY_GUIDE.md`, "Understand the city case before sharing it."],
    ],
  },
  {
    slug: "boston",
    eyebrow: "The bundled study city",
    title: "Boston: an invitation to investigate.",
    deck: "See the real value of an open research workspace: an inspectable city study that makes its layers, reasoning, and limits available to everyone.",
    image: "screenshots/city-detail.png",
    imageAlt: "Boston city-detail experience with map and evidence panels",
    primary: ["Open the Boston study", `${main}cities/boston`],
    secondary: ["Read the study guide", `${config.repositoryUrl}/blob/main/docs/BOSTON_STUDY_GUIDE.md`],
    sections: [
      ["What is here", "Boston is the real bundled study case: a boundary plus documented bottleneck and low-cooling-access overlays. It is a strong learning and inspection environment, not a procurement-ready optimizer."],
      ["What to ask", "Which supplied layers create this pattern? What dates, spatial resolution, and missing conditions matter? Where does community experience challenge or deepen the map?"],
      ["What comes next", "Bring a finding into a local conversation, validate it with public-health, engineering, and community expertise, then decide what further evidence is needed."],
    ],
    links: [
      ["Explore Boston interactively", `${main}cities/boston`, "Start with the visual evidence and plain-language explanation."],
      ["Open transparent scenarios", `${main}scenarios?cityId=boston`, "Use scenario outputs as questions for discussion, never as a promise."],
      ["Boston classroom guide", `${config.repositoryUrl}/blob/main/docs/BOSTON_CLASSROOM_GUIDE.md`, "A facilitation-ready learning path."],
      ["Evidence and responsible use", `${config.repositoryUrl}/blob/main/docs/wiki/04-evidence-and-responsible-use.md`, "The rules for interpreting and sharing a result."],
    ],
  },
  {
    slug: "science",
    eyebrow: "Methods without mystique",
    title: "Rigorous math. Legible meaning.",
    deck: "The platform makes network, spectral, resistance, reliability, percolation, and raster methods visible as reasoning tools—not as a black box that outranks local knowledge.",
    image: "screenshots/scenarios.png",
    imageAlt: "Scenario science and evidence experience",
    primary: ["Explore the science in the app", `${main}scenarios`],
    secondary: ["Read science and interpretation", `${config.repositoryUrl}/blob/main/docs/wiki/03-science-and-interpretation.md`],
    sections: [
      ["Model = lens", "A network depends on choices about spatial units, edges, weights, thresholds, and input layers. A result is useful when those choices are inspectable."],
      ["Bottlenecks are signals", "A Cheeger-style bottleneck identifies a weakly connected structure in the chosen model. It is not a diagnosis of a neighborhood, a person, or the correct intervention."],
      ["Uncertainty is part of the result", "The right response to a compelling pattern is to name the inputs, inference, gaps, and decision boundary—then test it locally."],
    ],
    links: [
      ["Explore the scenario workspace", `${main}scenarios`, "See assumptions, costs, and evidence labels alongside each option."],
      ["Science and interpretation", `${config.repositoryUrl}/blob/main/docs/wiki/03-science-and-interpretation.md`, "Read the method families and interpretation protocol."],
      ["Technical reference", `${config.repositoryUrl}/blob/main/docs/wiki/07-technical-reference.md`, "Find source modules, tests, contracts, and validation commands."],
      ["Research sharing strategy", `${config.repositoryUrl}/blob/main/docs/RESEARCH_SHARING_AND_PUBLICATION_STRATEGY.md`, "Learn how to cite and communicate a release responsibly."],
    ],
  },
  {
    slug: "community",
    eyebrow: "Community knowledge is evidence",
    title: "No heat decision without public context.",
    deck: "A city is more than a boundary file. Responsible urban-heat work begins with local purpose, partnership, accessible communication, and a way to challenge what the model misses.",
    image: "screenshots/cities.png",
    imageAlt: "City evidence and readiness experience",
    primary: ["Explore city readiness", `${main}cities`],
    secondary: ["Read the partnership guide", `${config.repositoryUrl}/blob/main/docs/wiki/05-city-onboarding-and-partnership.md`],
    sections: [
      ["Partnership before deployment", "Set a public-interest purpose, identify local interpreters, agree on data governance, and create a process for residents to surface omissions and harms."],
      ["Readiness must be visible", "A boundary-only city can support orientation. It cannot support heat conclusions. Evidence maturity should shape the confidence and use of every view."],
      ["Accountability continues after launch", "Share what informed a decision, what was uncertain, who reviewed it, and what changed after public feedback."],
    ],
    links: [
      ["Explore cities in the platform", `${main}cities`, "See what each city experience is ready to support."],
      ["City onboarding and partnership", `${config.repositoryUrl}/blob/main/docs/wiki/05-city-onboarding-and-partnership.md`, "A practical civic and technical readiness guide."],
      ["Evidence and responsible use", `${config.repositoryUrl}/blob/main/docs/wiki/04-evidence-and-responsible-use.md`, "Protect people, not just data."],
      ["Community learning path", `${main}modes`, "Choose a guided way into the workspace."],
    ],
  },
];

const absolute = (path) => (path ? `${pages}${path}/` : pages);
const card = ([title, text], index) => `<article class="principle-card"><span class="card-index">0${index + 1}</span><h2>${esc(title)}</h2><p>${esc(text)}</p></article>`;
const resource = ([title, href, text]) => `<a class="resource-link" href="${esc(href)}"><span><strong>${esc(title)}</strong><small>${esc(text)}</small></span><b aria-hidden="true">↗</b></a>`;

const wikiArticles = [
  { slug: "worked-example", title: "A Practical Graph-Theory Example", deck: "Follow four map cells from a simple connection diagram to a careful, useful public question—without needing advanced mathematics.", body: `<figure class="process-figure"><img src="${pages}assets/cheeger-cut-walkthrough.svg" alt="Four-step diagram: four map cells become a weighted graph; A is selected as a cooling sink; a dashed Cheeger cut crosses the weak B-to-C edge; conductance is one ninth; the outcome is a question for local investigation."><figcaption>Read top to bottom: model cells, stated weights, inferred sink, candidate cut, then a careful public question.</figcaption></figure><h2>A tiny city map</h2><p>Imagine four equal map cells along a route to a shaded park. A is a candidate cooling sink. The numbers are relative <strong>conductances</strong>: larger means the model treats a connection as easier.</p><div class="equation">A (park) — 4 — B — 1 — C — 4 — D</div><p>The B–C link has weight 1; the outer links have weight 4. The number is not a temperature, distance, or judgement about people. It is one stated model assumption.</p><h2>Find the seam</h2><p>Put A and B on one side of a proposed split, <em>S={A,B}</em>. Only one edge crosses to the other side: B–C, with weight 1. The weighted degree volume on each side is 9, so the conductance is:</p><div class="equation">φ(S) = cut(S,V∖S) / min(vol(S),vol(V∖S)) = 1/9 ≈ 0.11</div><p>That small result says the halves are weakly connected <em>in this four-cell graph</em>. If the B–C connection were changed from 1 to 4, the result becomes <em>4/12 ≈ 0.33</em>: the seam is less pronounced.</p><h2>Translate math into a public question</h2><div class="callout"><strong>The right question:</strong> What conditions around this transition—canopy, shade timing, safe routes, public access, maintenance, or resident experience—might make cooling continuity difficult, and what local evidence would change that answer?</div><p>The graph does not say that a place is dangerous, that residents lack cooling, or that a project will deliver a stated temperature reduction. It shows why this connection deserves careful local investigation. Read the longer <a href="${config.repositoryUrl}/blob/main/docs/wiki/08-graph-theory-worked-example.md">repository worked example</a> or return to the <a href="${main}">canonical platform</a>.</p>` },
  { slug: "", title: "The Urban Heat Research Wiki", deck: "A rigorous, public-interest companion to the living urban-heat workspace: methods, equations, assumptions, and decision boundaries in one readable place.", body: `<div class="wiki-grid"><a class="wiki-card" href="${pages}wiki/math/"><p class="eyebrow">Mathematical foundations</p><h2>From a heat raster to a defensible graph</h2><p>Spatial fields, adjacency rules, Laplacians, conductance, effective resistance, uncertainty propagation, and validation logic.</p></a><a class="wiki-card" href="${pages}wiki/interpretation/"><p class="eyebrow">Scientific interpretation</p><h2>How to read a signal without mistaking it for a verdict</h2><p>What each layer can support, what it cannot, and the evidence needed before action.</p></a><a class="wiki-card" href="${pages}wiki/technical/"><p class="eyebrow">Reproducibility</p><h2>Trace every result to inputs, code, and tests</h2><p>Technical contracts, source modules, validation commands, and artifact provenance.</p></a><a class="wiki-card" href="${main}"><p class="eyebrow">Living workspace</p><h2>See the methods in public</h2><p>Return to the canonical platform to explore Boston evidence and transparent scenarios.</p></a></div>` },
  { slug: "math", title: "Mathematical Foundations", deck: "A careful, equation-level explanation of the spatial and network reasoning used to turn urban-heat evidence into inspectable questions.", body: `<h2>1. Start with a spatial field, not a story</h2><p>Let <em>T(x)</em> denote a temperature-related surface at location <em>x</em>. A raster observes cell summaries, not a continuous city: <em>yᵢ = Aᵢ[T] + εᵢ</em>, where <em>Aᵢ</em> represents the sensor, pixel footprint, resampling, masking, and retrieval choices, and <em>εᵢ</em> collects measurement and processing error. This distinction matters: a map value is a documented estimate, not a direct reading of every person’s exposure.</p><div class="equation">yᵢ = Aᵢ[T] + εᵢ   ;   E[εᵢ | xᵢ] = 0 is an assumption to test, not a guarantee.</div><h2>2. Construct a graph transparently</h2><p>Partition the study area into cells or features <em>V</em>. Create an edge <em>(i,j)</em> only when the chosen adjacency rule says the units are connected—rook, queen, distance radius, street network, or a documented hybrid. A nonnegative weight <em>wᵢⱼ</em> may express contiguity, similarity, or modeled cooling permeability. The degree is <em>dᵢ = Σⱼ wᵢⱼ</em>.</p><div class="equation">L = D − W   ;   Lᵣw = I − D⁻¹ᐟ² W D⁻¹ᐟ²</div><p>The graph Laplacian <em>L</em> formalizes smoothness. For any vector <em>f</em>, <em>fᵀLf = ½Σᵢⱼwᵢⱼ(fᵢ−fⱼ)²</em>. Large differences across strongly weighted edges cost more; that is a modeling statement about the edge definition, not an independent fact about the city.</p><h2>3. Locate structural bottlenecks</h2><p>For a subset <em>S</em>, conductance compares the edge boundary to the modeled volume on its smaller side. A small value identifies a weakly connected region in this particular graph.</p><div class="equation">φ(S) = cut(S,V∖S) / min(vol(S), vol(V∖S)),    vol(S)=Σᵢ∈S dᵢ</div><p>The second eigenvalue of the normalized Laplacian, <em>λ₂</em>, is related to the best conductance by Cheeger-type bounds: <em>λ₂/2 ≤ φ* ≤ √(2λ₂)</em>. These bounds give a principled reason to inspect a spectral partition; they do not prove that the area has the greatest lived heat burden or that one intervention is optimal.</p><h2>4. Model pathways with resistance</h2><p>With conductances collected in <em>L</em>, the effective resistance between nodes <em>a</em> and <em>b</em> is <em>Rₑff(a,b)=(eₐ−e_b)ᵀL⁺(eₐ−e_b)</em>, where <em>L⁺</em> is the Moore–Penrose pseudoinverse. It measures separation in the modeled network. If edge weights represent cooling connectivity, resistance is a useful friction proxy; if they do not, the same number has a different meaning.</p><div class="equation">Rₑff(a,b) = (eₐ − e_b)ᵀ L⁺ (eₐ − e_b)</div><h2>5. Make uncertainty travel with the result</h2><p>For a derived metric <em>g(y,θ)</em> with data <em>y</em> and modeling choices <em>θ</em>, local uncertainty can be approximated by the delta method: <em>Var[g] ≈ ∇gᵀΣ∇g</em>. In practice, rerunning across plausible resolutions, dates, thresholds, adjacency rules, and weights is often more honest than reporting false precision.</p><div class="equation">Var[g(y,θ)] ≈ ∇g(y,θ)ᵀ Σ ∇g(y,θ)</div><div class="callout"><strong>Decision rule:</strong> a pattern that changes materially under reasonable choices is a prompt for more evidence, not a ranking to operationalize.</div>` },
  { slug: "interpretation", title: "Interpretation & Responsible Use", deck: "The discipline of converting mathematical output into an accountable public question—without overclaiming.", body: `<h2>Evidence has a chain of custody</h2><p>Every claim should name the source, date, spatial support, retrieval or processing method, coverage gaps, and transformation. Separate the observation (what a layer records), the model output (what an algorithm computes), and the proposed action (what people decide). They are different kinds of statements with different burdens of proof.</p><h2>A practical inference ladder</h2><ol><li><strong>Observation:</strong> this supplied layer shows a spatial pattern under stated conditions.</li><li><strong>Analytical signal:</strong> this graph has a low-conductance partition under stated weights and adjacency.</li><li><strong>Hypothesis:</strong> local cooling access or exposure may deserve investigation here.</li><li><strong>Decision:</strong> a public body chooses action after local validation, feasibility, equity, engineering, and community review.</li></ol><h2>What validation looks like</h2><p>Triangulate with field observations, resident knowledge, public-health and emergency data where lawful and appropriate, shade and tree condition, housing and indoor conditions, mobility, maintenance, and local plans. Pre-register the question when possible; document disagreement; preserve the negative finding as well as the persuasive map.</p><div class="callout"><strong>Ethical boundary:</strong> no map or score should be used to infer an individual’s health status, deny a service, or substitute for consent and public process.</div><p>For the full repository record, consult the <a href="${repoDocs}/04-evidence-and-responsible-use.md">evidence and responsible-use guide</a>. Then return to the <a href="${main}">canonical Urban Heat platform</a> to inspect the living evidence beside its caveats.</p>` },
  { slug: "technical", title: "Technical Reference & Reproducibility", deck: "A direct route from public-facing claims to the code, inputs, contracts, and tests that make them auditable.", body: `<h2>Reproducibility is a public feature</h2><p>The scientific core separates graph, spectral, resistance, reliability, percolation, raster, and report workflows. The executable source is the implementation record; this wiki is the reading guide.</p><ul><li><a href="${config.repositoryUrl}/tree/main/core">Scientific core modules</a> define graph construction, spectral calculations, raster utilities, and reporting.</li><li><a href="${config.repositoryUrl}/tree/main/tests">Tests</a> capture numerical expectations and contract behavior.</li><li><a href="${config.repositoryUrl}/blob/main/docs/BOSTON_STUDY_GUIDE.md">Boston Study Guide</a> states the bundled-study meaning and limits.</li><li><a href="${config.repositoryUrl}/blob/main/docs/Urban_Thermal_Math_Deep_Dive.md">Urban Thermal Math Deep Dive</a> provides the extended mathematical companion.</li></ul><h2>Minimum reproducibility record</h2><p>For each release, preserve input identifiers and licenses, boundary version, CRS, raster resolution and dates, masks and exclusions, graph rule, edge weights, parameter values, software revision, random seed, test results, and a plain-language interpretation statement. A result that cannot be reconstructed cannot be meaningfully challenged—and cannot earn public trust.</p><p>Open the <a href="${main}">canonical platform</a> to see the methods made legible in the actual urban-heat experience.</p>` },
];

function renderWiki(article) {
  const title = `${article.title} | ${config.projectName}`;
  const nav = `<a href="${wikiHome}">Wiki home</a><a href="${pages}wiki/worked-example/">Worked example</a><a href="${pages}wiki/math/">Math</a><a href="${pages}wiki/interpretation/">Interpretation</a><a href="${pages}wiki/technical/">Technical reference</a>`;
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(title)}</title><meta name="description" content="${esc(article.deck)}"><meta name="author" content="${esc(config.authorName)}"><meta name="robots" content="index,follow"><link rel="canonical" href="${main}"><link rel="stylesheet" href="${pages}assets/field-guide.css"></head><body><a class="skip-link" href="#main">Skip to article</a><header class="site-header"><a class="wordmark" href="${pages}"><span>Urban Heat</span> Democratization</a><nav aria-label="Research wiki">${nav}</nav><a class="header-cta" href="${main}">Open platform ↗</a></header><main id="main"><article class="article ${article.slug ? "" : "wiki-hero"}"><p class="eyebrow">Urban Heat Democratization · Research wiki</p><h1>${esc(article.title)}</h1><p class="deck">${esc(article.deck)}</p>${article.body}</article></main><footer><div><a class="wordmark" href="${main}"><span>Urban Heat</span> Democratization</a><p>Authored by <a href="${config.contactUrl}">${esc(config.authorName)}</a> · Open public-interest research.</p></div><div><a href="${main}">Canonical platform</a><a href="${repoDocs}">Repository wiki</a></div></footer></body></html>`;
}

function renderPage(page) {
  // The Pages guide is a discovery surface. The custom domain is the authoritative
  // public identity, so search engines and readers always return to it.
  const canonical = main;
  const title = `${page.title} | ${config.projectName}`;
  const schema = JSON.stringify({
    "@context": "https://schema.org", "@type": "WebPage", name: title, description: page.deck, url: canonical,
    about: ["Urban heat", "Heat equity", "Climate resilience", "Public-interest research"],
    author: { "@type": "Person", name: config.authorName }, isPartOf: { "@type": "WebSite", name: config.projectName, url: main }, mainEntityOfPage: main,
  });
  const nav = guides.map((guide) => `<a ${guide.slug === page.slug ? 'aria-current="page"' : ""} href="${absolute(guide.slug)}">${guide.slug ? esc(guide.slug) : "Field guide"}</a>`).join("");
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(title)}</title><meta name="description" content="${esc(page.deck)}"><meta name="robots" content="index,follow,max-image-preview:large,max-snippet:-1"><link rel="canonical" href="${canonical}"><meta property="og:type" content="website"><meta property="og:title" content="${esc(title)}"><meta property="og:description" content="${esc(page.deck)}"><meta property="og:url" content="${canonical}"><script type="application/ld+json">${schema.replaceAll("<", "\\u003c")}</script><link rel="stylesheet" href="${pages}assets/field-guide.css"></head><body><a class="skip-link" href="#main">Skip to the guide</a><header class="site-header"><a class="wordmark" href="${pages}"><span>Urban Heat</span> Democratization</a><nav aria-label="Field guide">${nav}</nav><a class="header-cta" href="${main}">Open platform <span aria-hidden="true">↗</span></a></header><main id="main"><section class="hero"><div class="hero-copy"><p class="eyebrow">${esc(page.eyebrow)}</p><h1>${esc(page.title)}</h1><p class="deck">${esc(page.deck)}</p><div class="hero-actions"><a class="button primary" href="${esc(page.primary[1])}">${esc(page.primary[0])} <span aria-hidden="true">↗</span></a><a class="button quiet" href="${esc(page.secondary[1])}">${esc(page.secondary[0])}</a></div><p class="credibility-note">A public-interest workspace for seeing local heat patterns, understanding the evidence, and helping shape thoughtful action on cooling, shade, and public investment.</p></div><figure class="hero-visual"><img src="${pages}assets/${page.image}" alt="${esc(page.imageAlt)}" loading="eager"><figcaption>Explore the living workspace; cite the underlying methods and evidence.</figcaption></figure></section><section class="principles" aria-label="Key ideas">${page.sections.map(card).join("")}</section><section class="path"><div><p class="eyebrow">A clear next step</p><h2>Go from curiosity to an informed question.</h2><p>Use the interactive experience to investigate the evidence, then return to the methods and sources before treating any output as a conclusion.</p></div><a class="button primary" href="${main}">Explore Urban Heat Democratization <span aria-hidden="true">↗</span></a></section><section class="resources"><div><p class="eyebrow">Continue your research</p><h2>Primary sources, not a content maze.</h2></div><div class="resource-grid">${page.links.map(resource).join("")}</div></section></main><footer><div><a class="wordmark" href="${main}"><span>Urban Heat</span> Democratization</a><p>Created by <a href="${config.contactUrl}">${esc(config.authorName)}</a> · Open public-interest research.</p></div><div><a href="${main}">Main platform</a><a href="${wikiHome}">Research wiki</a><a href="${config.repositoryUrl}">Source repository</a></div></footer></body></html>`;
}

const css = `:root{--ink:#102728;--muted:#587174;--paper:#f6f7f1;--line:#d9e2db;--ocean:#075f72;--gold:#d99a35;--shadow:0 24px 80px rgba(13,43,43,.12)}*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;background:radial-gradient(circle at 92% -10%,#d6eee8 0,transparent 31rem),var(--paper);color:var(--ink);font:16px/1.6 Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}.skip-link{position:fixed;left:1rem;top:-4rem;z-index:10;background:#fff;padding:.6rem 1rem;color:var(--ink)}.skip-link:focus{top:1rem}.site-header,footer{display:flex;align-items:center;justify-content:space-between;gap:1.5rem;max-width:1320px;margin:auto;padding:1.3rem clamp(1.2rem,4vw,4rem)}.site-header{border-bottom:1px solid var(--line)}.wordmark{color:var(--ink);font-size:.95rem;font-weight:800;letter-spacing:.025em;text-decoration:none;white-space:nowrap}.wordmark span{color:var(--ocean)}nav{display:flex;gap:1rem;align-items:center}nav a,footer a{color:var(--muted);font-size:.86rem;font-weight:700;text-decoration:none;text-transform:capitalize}nav a:hover,nav a[aria-current=page],footer a:hover{color:var(--ocean)}.header-cta{color:var(--ocean);font-size:.85rem;font-weight:800;text-decoration:none;white-space:nowrap}main{max-width:1320px;margin:auto;padding:clamp(3rem,7vw,7rem) clamp(1.2rem,4vw,4rem) 4rem}.hero{display:grid;grid-template-columns:minmax(0,1.08fr) minmax(310px,.92fr);align-items:center;gap:clamp(2rem,6vw,7rem);min-height:32rem}.eyebrow{margin:0 0:.7rem;color:var(--ocean);font-size:.72rem;font-weight:850;letter-spacing:.13em;text-transform:uppercase}.hero h1,.path h2,.resources h2{max-width:13ch;margin:0;color:#102d2d;font-family:Georgia,"Times New Roman",serif;font-size:clamp(3rem,6.4vw,6.5rem);font-weight:500;letter-spacing:-.055em;line-height:.91}.deck{max-width:39rem;margin:1.5rem 0;color:#395558;font-size:clamp(1.1rem,1.65vw,1.35rem);line-height:1.55}.hero-actions{display:flex;flex-wrap:wrap;gap:.75rem;margin:1.7rem 0}.button{display:inline-flex;align-items:center;justify-content:center;gap:.5rem;border:1px solid var(--ocean);border-radius:999px;padding:.78rem 1.15rem;font-weight:800;text-decoration:none}.button.primary{background:var(--ocean);color:#fff}.button.primary:hover{background:#034e60}.button.quiet{color:var(--ocean)}.credibility-note{max-width:38rem;margin:2rem 0 0;border-left:2px solid var(--gold);padding-left:.8rem;color:var(--muted);font-size:.86rem}.hero-visual{position:relative;margin:0}.hero-visual:before{position:absolute;inset:-1rem 1rem 1rem -1rem;z-index:-1;border-radius:1.3rem;background:#c4e3dc;content:""}.hero-visual img{display:block;width:100%;min-height:19rem;object-fit:cover;border:1px solid rgba(13,70,72,.12);border-radius:1rem;box-shadow:var(--shadow)}figcaption{margin-top:.7rem;color:var(--muted);font-size:.78rem}.principles{display:grid;grid-template-columns:repeat(3,1fr);gap:1rem;margin-top:6rem}.principle-card{min-height:17rem;padding:1.5rem;border:1px solid var(--line);border-radius:1rem;background:rgba(255,255,250,.8)}.card-index{color:var(--gold);font-size:.76rem;font-weight:900;letter-spacing:.12em}.principle-card h2{margin:2.2rem 0 .6rem;font-family:Georgia,serif;font-size:1.65rem;font-weight:500;line-height:1.08}.principle-card p{margin:0;color:var(--muted)}.path{display:flex;align-items:end;justify-content:space-between;gap:2rem;margin:5.5rem 0;padding:clamp(2rem,5vw,4rem);border-radius:1.2rem;background:#143b3d;color:#ecf3ed}.path h2{font-size:clamp(2.4rem,4vw,4.5rem);color:#fff}.path p:not(.eyebrow){max-width:48rem;color:#cfe0d9}.path .eyebrow{color:#f2c573}.path .button{flex:0 0 auto;border-color:#fff;background:#fff;color:#0f3b3c}.resources{display:grid;grid-template-columns:.8fr 1.2fr;gap:4rem;align-items:start;padding-bottom:2rem}.resources h2{font-size:clamp(2.2rem,3.6vw,3.6rem)}.resource-grid{display:grid;gap:.7rem}.resource-link{display:flex;justify-content:space-between;gap:1rem;padding:1.05rem 0;border-bottom:1px solid var(--line);color:var(--ink);text-decoration:none}.resource-link strong{display:block;font-size:1.05rem}.resource-link small{display:block;margin-top:.2rem;color:var(--muted)}.resource-link b{color:var(--ocean);font-size:1.2rem}.wiki-hero{max-width:60rem}.wiki-hero h1{font-size:clamp(3rem,6vw,6rem)}.wiki-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:1rem;margin-top:3rem}.wiki-card{display:block;padding:1.5rem;border:1px solid var(--line);border-radius:1rem;background:#fff;color:var(--ink);text-decoration:none;transition:transform .2s,box-shadow .2s}.wiki-card:hover{transform:translateY(-3px);box-shadow:var(--shadow)}.wiki-card p{color:var(--muted)}.article{max-width:52rem;margin:auto}.article h1{font-family:Georgia,serif;font-size:clamp(3rem,6vw,5.5rem);line-height:.94;letter-spacing:-.05em}.article h2{margin-top:3rem;font-family:Georgia,serif;font-size:2rem;line-height:1.1}.article p,.article li{color:#395558}.equation{margin:1.5rem 0;padding:1.35rem 1.5rem;border-left:3px solid var(--gold);border-radius:.5rem;background:#eaf3ef;color:#123f42;font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:1.08rem;overflow:auto}.callout{margin:2rem 0;padding:1.2rem 1.4rem;border:1px solid #b9d8d0;border-radius:.8rem;background:#f0f8f5}.article a{color:var(--ocean);font-weight:700}footer{margin-top:2rem;border-top:1px solid var(--line);align-items:flex-start}footer p{margin:.5rem 0 0;color:var(--muted);font-size:.8rem}footer>div:last-child{display:flex;flex-wrap:wrap;justify-content:flex-end;gap:1rem}@media(max-width:800px){.site-header nav{display:none}.site-header{padding-top:1rem;padding-bottom:1rem}.hero,.resources,.wiki-grid{grid-template-columns:1fr}.hero{min-height:auto}.hero h1{max-width:11ch}.hero-visual{max-width:38rem}.principles{grid-template-columns:1fr;margin-top:4rem}.principle-card{min-height:0}.path{align-items:start;flex-direction:column;margin:4rem 0}.resources{gap:2rem}footer{flex-direction:column}footer>div:last-child{justify-content:flex-start}}`;

await mkdir(resolve(out, "assets/screenshots"), { recursive: true });
await Promise.all(["home.png", "city-detail.png", "scenarios.png", "cities.png"].map((file) => copyFile(resolve(root, `docs/screenshots/${file}`), resolve(out, `assets/screenshots/${file}`))));
await copyFile(resolve(root, "docs/wiki/assets/cheeger-cut-walkthrough.svg"), resolve(out, "assets/cheeger-cut-walkthrough.svg"));
await writeFile(resolve(out, ".nojekyll"), "");
await copyFile(resolve(root, "web/public/UHD-INDEXNOW-2026-AARTI-7F3C9D2E5B8A1C4D.txt"), resolve(out, "UHD-INDEXNOW-2026-AARTI-7F3C9D2E5B8A1C4D.txt"));
await writeFile(resolve(out, "assets/field-guide.css"), `${css}.process-figure{margin:2rem 0}.process-figure img{display:block;width:100%;height:auto;border:1px solid var(--line);border-radius:1rem;background:#f6f7f1}.process-figure figcaption{margin-top:.7rem;color:var(--muted);font-size:.86rem}`);
await writeFile(resolve(out, "robots.txt"), `User-agent: *\nAllow: /\nSitemap: ${pages}sitemap.xml\n`);
await writeFile(resolve(out, "sitemap.xml"), `<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${[...guides.map((page) => absolute(page.slug)), ...wikiArticles.map((article) => `${wikiHome}${article.slug ? `${article.slug}/` : ""}`)].map((url) => `<url><loc>${url}</loc><changefreq>monthly</changefreq><priority>0.6</priority></url>`).join("")}</urlset>\n`);
await Promise.all(guides.map(async (page) => { const directory = page.slug ? resolve(out, page.slug) : out; await mkdir(directory, { recursive: true }); await writeFile(resolve(directory, "index.html"), renderPage(page)); }));
await Promise.all(wikiArticles.map(async (article) => { const directory = article.slug ? resolve(out, "wiki", article.slug) : resolve(out, "wiki"); await mkdir(directory, { recursive: true }); await writeFile(resolve(directory, "index.html"), renderWiki(article)); }));
console.log(`Generated ${guides.length} GitHub Pages field-guide pages in ${out}`);
